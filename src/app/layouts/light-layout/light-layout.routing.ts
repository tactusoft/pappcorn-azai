import { Routes } from '@angular/router';

import { LoginComponent } from 'app/login/login.component';

export const LightLayoutRoutes: Routes = [
    { path: 'login',      component: LoginComponent },
];
